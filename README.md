# SPML-HW1
```bash=
# You can run code with the following command:

python hw1-1.py --data_path <evaluation_set> --output_path <path for saving adversarial image> --batch_size <batch_size> --task <[adv_img, adv_img2]>
```